<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>PokeDex</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet"> 
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <style>
        body {
            font-family: 'Press Start 2P', cursive;
            background: #f0f0f0;
            color: #333;
            text-align: center;
            margin: 0;
            padding: 0;
        }
        h1 {
            color: #ffcb05; 
            font-size: 3rem;
            margin-top: 20px;
        }
        a {
            display: inline-block;
            color: #fff;
            background: #007bff;
            text-decoration: none;
            border-radius: 15px;
            padding: 10px 20px;
            font-size: 1.2rem;
            margin: 10px;
            transition: background 0.3s, transform 0.3s;
        }
        a:hover {
            background: #0056b3;
            transform: scale(1.05);
        }
        .pokemon-list {
            display: block;
            flex-wrap: wrap;
            justify-content: center;
            
        }
        .pokemon-item {
            background: #fff;
            border: 2px solid #ddd;
            border-radius: 15px;
            margin: 10px;
            padding: 15px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            transition: box-shadow 0.3s, transform 0.3s;
            text-align: center;
        }
        .pokemon-item:hover {
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
            transform: scale(1.05);
        }
        .pokemon-item img {
            width: 100px; 
            height: auto;
            display: block;
            margin: 0 auto 10px;
        }
    </style>
</head>
<body>
    <h1>PokeDex Unicesumar</h1>
    <div class="pokemon-list container mt-3">
        <% results.forEach(pokemon => { %>
            <div class="pokemon-item">
                <th><%= pokemon.id %></th>
                <% 
                    // Generate the Pokémon image URL
                    const pokemonId = pokemon.url.split('/')[6];
                    const pokemonImageUrl = `https://raw.githubusercontent.com/PokeAPI/sprites/master/sprites/pokemon/other/official-artwork/${pokemonId}.png`;
                %>
                <img src="<%= pokemonImageUrl %>" alt="<%= pokemon.name %>">
                <a href="http://localhost:3000/<%= pokemon.name %>"> <%= pokemon.name %> </a>
                
            </div>
        <% }) %>
    </div>
</body>
</html>
